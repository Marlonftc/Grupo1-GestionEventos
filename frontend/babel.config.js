module.exports = {
  // 🔹 Definimos los presets que usará Babel para compilar el código de Vue
  presets: [
    '@vue/cli-plugin-babel/preset' // 🔹 Usa el preset de Vue CLI para transformar código moderno de Vue a JavaScript compatible con más navegadores
  ]
}
